name: publish

on:
  release:
    types:
      - published

jobs:
  publish:
    uses: coatl-dev/workflows/.github/workflows/pypi-upload.yml@v7
    secrets:
      password: ${{ secrets.PYPI_API_TOKEN }}
