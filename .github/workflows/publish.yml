name: publish

on:
  release:
    types:
      - published

jobs:
  tox:
    uses: coatl-dev/workflows/.github/workflows/tox.yml@v1

  pypi-publish:
    needs:
      - tox
    uses: coatl-dev/workflows/.github/workflows/pypi-upload.yml@v1
    secrets:
      password: ${{ secrets.PYPI_API_TOKEN }}
