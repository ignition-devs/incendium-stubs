name: publish

on:
  release:
    types:
      - published

jobs:
  tox:
    uses: coatl-dev/workflows/.github/workflows/tox.yml@v7

  pypi-publish:
    needs:
      - tox
    uses: coatl-dev/workflows/.github/workflows/pypi-upload.yml@v7
    secrets:
      password: ${{ secrets.PYPI_API_TOKEN }}
